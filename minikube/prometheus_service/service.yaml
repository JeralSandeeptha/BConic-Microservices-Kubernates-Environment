apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: production
spec:
  selector:
    app: prometheus-pod
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090