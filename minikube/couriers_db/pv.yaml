apiVersion: v1
kind: PersistentVolume
metadata:
  name: couriers-db-pv
  namespace: production
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Recycle
  storageClassName: standard
  hostPath:
    path: /mnt/data/postgres_data_couriers