apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: couriers-db-pvc
  namespace: production
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi